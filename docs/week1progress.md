For Week 1, I completed the initial analysis and pushed the first working prototype to GitHub. 
The prototype includes a small incident severity dataset, a baseline AI model training script, and a FastAPI service that exposes the model through 
an API. I tested the prediction endpoint successfully, and the API returned the predicted severity, 
model version, and latency. This establishes the first AI-as-a-Service flow for the project and creates the 
foundation for adding scaling, monitoring, model versioning, shadow deployment, canary rollout, and rollback logic in future weeks.
